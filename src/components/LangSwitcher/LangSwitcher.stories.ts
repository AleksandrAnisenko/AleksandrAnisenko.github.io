import type { Meta } from '@storybook/react';
import { LangSwitcher } from './LangSwitcher';

const meta: Meta<typeof LangSwitcher> = {
  title: 'Example/LangSwitcher',
  component: LangSwitcher,
  tags: ['autodocs'],
};

export default meta;

export const MainLangSwitcher = {};