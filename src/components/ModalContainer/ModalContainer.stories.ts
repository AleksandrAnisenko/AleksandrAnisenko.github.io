import type { Meta } from '@storybook/react';
import { ModalContainer } from './ModalContainer';

const meta: Meta<typeof ModalContainer> = {
  title: 'Example/ModalContainer',
  component: ModalContainer,
  tags: ['autodocs'],
};

export default meta;

export const MainModalContainer = {};
